<h3><%= link_to 'Add an entry', new_entry_path %></h3>
<h1>All dictionary entries:</h1>

<% @entries.each do |entry| %>
  <div class="entry">
    <%= link_to "#{entry.word}", entry_path(entry.id) %>
  </div>
<% end %>
